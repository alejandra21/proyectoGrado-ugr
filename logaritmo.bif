module Math;

function logaritmo%(x: double%) : double
    %{

    #include <stdio.h>
    #include <math.h>

    double result = log(x);
    //printf("Resultado = ", result);
    
    return new Val(result, TYPE_DOUBLE);

    %}
